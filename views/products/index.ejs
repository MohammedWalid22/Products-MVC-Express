<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="/css/style.css">
  <title>Products</title>
</head>
<body>

<h1>Products</h1>
<a href="/products/new">➕ Add Product</a>

<div class="grid">
  <% products.forEach(p => { %>
    <div class="card">
      <h3><%= p.name %></h3>
      <p><%= p.description %></p>
      <strong><%= p.price %> EGP</strong>
      <div class="actions">
        <a href="/products/<%= p._id %>/edit">✏️</a>
        <form action="/products/<%= p._id %>/delete" method="POST">
          <button>🗑</button>
        </form>
      </div>
    </div>
  <% }) %>
</div>

</body>
</html>
